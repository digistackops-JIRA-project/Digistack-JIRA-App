# Copy to .env.local for local development — NEVER commit to Git
# Vite exposes only vars prefixed with VITE_

# Backend API base URL (leave empty to use nginx proxy in Docker)
# For local npm run dev, set this to your backend URL:
VITE_API_BASE_URL=http://localhost:8000
